# See https://wiki.hyprland.org/Configuring/Monitors/
# Configured by A La Carchy - multi-monitor layout
# Format: monitor = name, resolution, position, scale

env = GDK_SCALE,1

monitor=DP-2,preferred,1920x0,1.00,transform,0
monitor=DP-1,preferred,0x0,1.00,transform,0

# Fallback for hot-plugged displays
monitor=,preferred,auto,1
